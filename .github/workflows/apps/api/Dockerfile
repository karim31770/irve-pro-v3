FROM node:20-alpine
WORKDIR /app
COPY . .
# ⚠️ À adapter à ton projet (npm/pnpm, build, etc.)
# Pour l’instant, c’est un placeholder qui ne marchera que si ton API est déjà prête.
EXPOSE 4010
CMD ["node", "dist/main.js"]
